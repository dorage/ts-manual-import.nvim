local import = require("ts-manual-import.module")

describe("add import statements", function()
  it("in the blank file", function() end)
  it("in the file, that have no import statements", function() end)
  it("in the file, that have import statements, but sources are not overlapped", function() end)
  it("in the file, that have import statements, but sources are overlapped", function() end)
end)
